# --- Core (model + data loading) ---
numpy>=1.24
torch>=2.0
torchvision>=0.15
Pillow>=10.0
tqdm>=4.65

# --- Data download (Kaggle API) ---
kaggle>=1.6

# --- Cloud training on Modal A100 (added when we get to training) ---
# modal>=0.63

# Note: heavy training runs inside a Modal-defined image with its own pinned
# deps; this file is mainly for local data-prep + testing the data loaders.
